{
  "name": "aurora-autism-agent",
  "version": "1.0.0",
  "description": "Aurora - Virtual Autism Support Assistant",
  "main": "server.js",
  "scripts": {
    "start": "PORT=3002 node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "setup": "echo 'Welcome to Aurora! 🧩' && echo 'Please create a .env file with your OPENAI_API_KEY' && echo 'Then run: npm run dev' && echo 'Visit: http://localhost:3002'",
    "health": "curl -s http://localhost:3002/health | json_pp || echo 'Aurora not running. Start with: npm run dev'"
  },
  "keywords": [
    "autism",
    "health",
    "assistant",
    "ai",
    "support"
  ],
  "author": "Aurora Team",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "multer": "^2.0.2",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
